version: 2
jobs:
  build:
    docker:

    - image: circleci/node:10-browsers-legacy

    working_directory: ~/repo

    steps:
    - checkout

    # Download and cache dependencies
    - restore_cache:
        keys:
        - v1-dependencies-{{ checksum "package.json" }}
        # fallback to using the latest cache if no exact match is found
        - v1-dependencies-

    - run:
        name: Install dependencies
        command: npm ci

    - save_cache:
        paths:
        - node_modules
        key: v1-dependencies-{{ checksum "package.json" }}

    #    - run:
    #        name: Run Linter
    #        command: npm run lint

    - run:
        name: Install flow type
        command: npm run flow:typed

    - run:
        name: Run flow
        command: npm run flow

  deploy_prod:
    docker:
    - image: circleci/node:10-browsers-legacy

    steps:
    - checkout

    - add_ssh_keys:
        fingerprints:
        - $SSH_FINGERPRINT

    - deploy:
        name: Deploy to server
        command: |
          echo '157.230.28.204 ecdsa-sha2-nistp256 AAAAE2VjZHNhLXNoYTItbmlzdHAyNTYAAAAIbmlzdHAyNTYAAABBBApRwfz3H5y2roV11fjQTZTuhD8nnA5a8XHCV/widp4MPkFyg3Odmd5ggUmhnjyP7bRzay0T7aiqWJcaUn5nUnw=' >> ~/.ssh/known_hosts
          ssh -o StrictHostKeyChecking=no 157.230.28.204
          ssh -v $DROPLET_USER@$DROPLET_IP


workflows:
  version: 2
  build_deploy:
    jobs:
    - build
    - deploy_prod:
        requires:
        - build
        filters:
          branches:
            only: master


